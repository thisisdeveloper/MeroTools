// Store listing links for the "Rate Us" prompt and Settings button.
// Package/bundle id: np.com.kumarusnil.merotools

// Live on Play Store — safe to use directly.
export const PLAY_STORE_URL =
  'https://play.google.com/store/apps/details?id=np.com.kumarusnil.merotools';

// TODO(user): fill this in once the iOS App Store listing is live —
// needs the numeric App Store ID Apple assigns at submission, e.g.
// https://apps.apple.com/app/id<NUMERIC_APP_STORE_ID>. Left null rather
// than guessed; every consumer of this constant already handles null by
// hiding/disabling the iOS-specific action instead of opening a broken link.
export const APP_STORE_URL: string | null = null;
