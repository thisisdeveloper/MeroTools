node_modules/
build/
/dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Android / Capacitor
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/.idea/
*.jks
*.keystore

# Vendored native plugins keep their own node_modules out of git — only
# the plugin source/patches (native-plugins/*/android, ios, dist, etc.)
# should be committed; npm reinstalls this via the file: dependency.
native-plugins/*/node_modules/
native-plugins/*/.swiftpm/
xcuserdata/
